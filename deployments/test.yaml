kind: Deployment
apiVersion: apps/v1
metadata:
  name: hello-world
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      containers:
        - name: hello-world
          image: localhost:32000/hello-world-arm-v9
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
      restartPolicy: Always
---
kind: Service
apiVersion: v1
metadata:
  name: hello-world
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      nodePort: 31000
  selector:
    app: hello-world